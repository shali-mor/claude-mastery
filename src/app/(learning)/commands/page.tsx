import { CommandBrowser } from '@/components/commands/CommandBrowser';

export const metadata = { title: 'Command Browser — Claude Mastery' };

export default function CommandsPage() {
  return <CommandBrowser />;
}
