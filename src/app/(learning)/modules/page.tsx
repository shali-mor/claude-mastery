import { ModuleIndex } from '@/components/modules/ModuleIndex';

export const metadata = { title: 'Learning Modules — Claude Mastery' };

export default function ModulesPage() {
  return <ModuleIndex />;
}
